#!/usr/bin/env python3
'''
Tuples are very similar to lists except that its contents can't be changed.
It is immutable though it can contain mutable elements.
'''

car = ("Toyota", "Camry", 2021, "Blue")

print(car)
print(len(car))
print(car[1])